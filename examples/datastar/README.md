# Datastar

A counter driven by Datastar: the browser sends its signals to the server, which answers with server-sent events that patch the signal and append an element.

Run it with:

```sh
cargo topcoat dev -p datastar
```
