# Runtime

Interactive pages built with the browser runtime: signals holding state, `@` event handlers updating them, expressions re-rendering on change, and a `:` bind attribute.

Run it with:

```sh
cargo topcoat dev -p runtime
```
