# Suspense

Streams page sections in behind fallbacks with the `suspense` and `error_boundary` components: a quote that swaps in when ready, a dashboard that turns a failed widget into a message while the rest renders, and a profile page whose session check redirects mid-stream through a client-side navigation.

Run it with:

```sh
cargo topcoat dev -p suspense
```
