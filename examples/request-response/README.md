# Request and response

Read request bodies and return HTTP responses using built-in and custom types.

Run it with:

```sh
cargo topcoat dev -p request-response
```

The example only serves API routes under `/api`; there is no page at `/`.
