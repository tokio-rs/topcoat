# Module router

Routes derived from the Rust module tree with `module_router!`, covering nested layouts, an underscore group that adds no URL segment, and a route.

Run it with:

```sh
cargo topcoat dev -p module-router
```
