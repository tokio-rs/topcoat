# Streaming

Pages that send their shell immediately and stream slow content in with `live!` and `emit!`: one page swaps a loading message for the data once it arrives, the other reports progress step by step.

Run it with:

```sh
cargo topcoat dev -p streaming
```
