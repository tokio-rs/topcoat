# Wrappers

Demonstrates the Wrapper component passing child content through to its inner section.

<Wrapper>
## Section Header

This paragraph is wrapped inside the Wrapper component. The wrapper adds section-level grouping and inner styling around its child content.

Multiple blocks of content can be nested inside a single Wrapper.
</Wrapper>
