# No Frontmatter

This page carries no frontmatter block at all, which `frontmatter = BlogPostMeta` allows: `meta()` answers `None` for it, and the layout falls back to the slug for a heading.

A directory does not have to be uniform. The listing skips this page because it has no date to sort by, but the route still works.
