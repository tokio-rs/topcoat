# Live

Pages that stream content with `live!` and `emit!`. The initial page appears while slow work continues, and later updates replace its placeholders.

Run it with:

```sh
cargo topcoat dev -p live
```
