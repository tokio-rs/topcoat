# Manual router

A router built without discovery: every layout, page, and route is registered by hand, including a nested layout and a plain-text API route.

Run it with:

```sh
cargo topcoat dev -p manual-router
```
