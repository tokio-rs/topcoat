# UI

A showcase of the components from `topcoat ui`, vendored into `src/components/` where they can be restyled and extended like any other source file.

Run it with:

```sh
cargo topcoat dev -p ui
```
