# Routerless

Render components and views to HTML strings without starting a server. The Topcoat dependency disables default features and enables only `view`.

Run it with:

```sh
cargo run -p routerless
```

