# Font

Two ways to use a web font: one picked from the Fontsource catalog and self-hosted as a Topcoat asset, one declared by hand with `font!` and loaded from a CDN.

Run it with:

```sh
cargo topcoat dev -p font
```
