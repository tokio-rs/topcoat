# The example `reg2` registry, exercising cross-registry dependencies.
# Hand-written: each component names its source file; hashes are computed from
# the source at runtime.

version = 1
name = "reg2"

[components.gadget]
source = "components/gadget.rs"
dependencies = [{ registry = "file://../reg3", name = "foo" }]
