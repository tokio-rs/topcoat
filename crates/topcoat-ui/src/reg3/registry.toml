# The example `reg3` registry, exercising cross-registry dependencies and an
# ambiguous component name (`button`). Hand-written: each component names its
# source file; hashes are computed from the source at runtime.

version = 1
name = "reg3"

[components.button]
source = "components/button2.rs"

[components.foo]
source = "components/foo.rs"
