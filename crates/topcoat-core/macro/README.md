The `#[memoize]` attribute macro for per-request caching of function results.

This crate is part of [`topcoat`](https://github.com/tokio-rs/topcoat). Application code should use the macro through `topcoat::context::memoize` instead of depending on this crate directly.
