[package]
name = "topcoat"
version = "0.1.0"
edition = "2024"

[features]
default = [
	"asset",
	"view",
	"router",
	"discover",
]

asset = ["dep:topcoat-asset"]
router = [
	"view",
	"dep:topcoat-router",
	"topcoat-macro/router",
]
tailwind = ["dep:topcoat-tailwind"]
view = [
	"dep:topcoat-view",
	"topcoat-asset/view",
	"topcoat-macro/view",
]

discover = [
	"dep:inventory",
	"topcoat-macro/discover",
	"topcoat-router/discover",
	"topcoat-view/discover",
]

[dependencies]
topcoat-asset = { workspace = true, optional = true }
topcoat-core.workspace = true
topcoat-macro.workspace = true
topcoat-router = { workspace = true, optional = true }
topcoat-tailwind = { workspace = true, optional = true }
topcoat-view = { workspace = true, optional = true, features = ["axum"] }

axum.workspace = true
futures-util.workspace = true
inventory = { workspace = true, optional = true }
serde = { workspace = true, features = ["derive"] }
serde_urlencoded.workspace = true
tokio.workspace = true
tokio-tungstenite.workspace = true

[lints]
workspace = true
