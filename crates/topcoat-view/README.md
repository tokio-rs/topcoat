Runtime types for Topcoat views: the `View` trait, rendered `ViewHandle`s, and the traits that decide how values render in each position of a `view!` template.

This crate is part of [`topcoat`](https://github.com/tokio-rs/topcoat). Application code should depend on the `topcoat` crate instead of using this crate directly.
