fn classes() {
    let classes = class!(
        "inline-flex items-center justify-center",
        "rounded-md text-sm font-medium",
        "transition-colors focus-visible:outline-none" if focused,
        variant_classes,
        "pointer-events-none opacity-50" if disabled else "cursor-pointer"
    );
}
