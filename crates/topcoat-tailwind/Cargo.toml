[package]
name = "topcoat-tailwind"
version = "0.1.0"
edition = "2024"

[features]
default = ["build"]
build = ["dep:ureq"]

[dependencies]
ureq = { workspace = true, optional = true }

[dev-dependencies]
topcoat = { workspace = true, features = ["tailwind"] }

[lints]
workspace = true
