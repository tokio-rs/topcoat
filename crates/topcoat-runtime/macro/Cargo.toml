[package]
name = "topcoat-runtime-macro"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
topcoat-runtime = { workspace = true, features = ["parsing"] }

syn = { workspace = true, features = ["full"] }

[lints]
workspace = true
