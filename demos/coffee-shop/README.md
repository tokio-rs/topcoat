# Coffee shop

A storefront where customers can browse drinks, search the menu, and place orders without reloading the page.

Run it with:

```sh
cargo topcoat dev -p coffee-shop
```

Start with [the app layout](src/app.rs), then follow the [menu page](src/app/menu.rs) into the components and data it uses.
